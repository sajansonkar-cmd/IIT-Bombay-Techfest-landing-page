# Model Asset

Place the cyborg GLB here:

```text
assets/models/cyborg.glb
```

The Three.js hero loads that file through `THREE.GLTFLoader`. A lightweight procedural fallback is shown during development if the GLB is unavailable.
